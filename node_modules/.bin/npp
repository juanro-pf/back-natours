#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../npp/cli/npp.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../npp/cli/npp.sh" "$@"
  ret=$?
fi
exit $ret
